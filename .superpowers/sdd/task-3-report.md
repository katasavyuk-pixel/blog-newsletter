# Task 3 — AchievementToast

**Status:** ✅ Done

## Files
- `src/components/home/achievement-toast.tsx` — created

## Build output
```
✓ Compiled successfully in 33.6s
✓ TypeScript finished — 0 errors
✓ All pages generated (37 static, 4 SSG)
```

## Summary
`AchievementToast` component implemented with `motion/react` animations, `prefers-reduced-motion` support via `useReducedMotion` hook, auto-dismiss after 4s, and `AnimatePresence` for exit transitions. Build passes with zero errors.
